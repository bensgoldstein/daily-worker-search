# AI-powered Daily Worker Search Database - Secrets Configuration
# Copy this file to secrets.toml and fill in your actual values
# NEVER commit secrets.toml to version control!

# Application password (shared by all users)
APP_PASSWORD_HASH = "YOUR_PASSWORD_HASH_HERE"  # Generate with: python auth.py yourpassword

# Session settings
SESSION_TIMEOUT_MINUTES = 60

# Pinecone API credentials
PINECONE_API_KEY = "YOUR_PINECONE_KEY_HERE"
PINECONE_HOST = "YOUR_PINECONE_HOST_HERE"

# Google Gemini API (for AI summaries)
GEMINI_API_KEY = "YOUR_GEMINI_KEY_HERE"

# Usage limits (adjust based on your budget)
MAX_SEARCHES_PER_HOUR = 100
MAX_SEARCHES_PER_DAY = 500
MAX_PDF_DOWNLOADS_PER_DAY = 50

# Cost control
DAILY_COST_LIMIT = 5.0  # Maximum estimated cost per day in USD